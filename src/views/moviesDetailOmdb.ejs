<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>

<figure class="text-center my-5">
    <img src="/img/logo-DH.png" alt="Logo Digital House" class="img-fluid rounded mx-auto d-block w-25">
</figure>

<% if (movie.Title && movie.Year && movie.Awards && movie.Runtime && movie.Genre && movie.Poster) { %>
    <div class="card mb-3 bg-danger mx-auto" style="max-width: 540px;">
        <div class="row g-0">
            <div class="col-md-4">
                <img class="img-fluid rounded-start" src="<%= movie.Poster %>" alt="<%= movie.Title %>">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h4 class="card-title"><%= movie.Title %></h4>
                    <h6 class="card-text">Año: <%= movie.Year %></h6>
                    <h6 class="card-text">Premios: <%= movie.Awards %></h6>
                    <h6 class="card-text">Duración: <%= movie.Runtime%></h6>
                    <h6 class="card-text">Género: <%= movie.Genre%></h6>
                </div>
            </div>
        </div>
    </div>
<% } else { %>
    <div class="text-center">
        <p>No se encontraron datos de la película.</p>
    </div>
<% } %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

